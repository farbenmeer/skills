---
name: prepare-spec
description: Clarify the spec until it is sufficient to make a sufficiently well-defined plan.
---

# Spec Preparation

Use git to find the current (unstaged) changes in the spec-directory.
If given a specific commit hash, use it to find spec changes in that commit.

Ask the user questions until the spec is sufficiently clear to make a well-defined plan. Adjust the Spec accordingly after every question.
